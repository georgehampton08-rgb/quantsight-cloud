# QuantSight Cloud Backend - Mobile API Dependencies
# =====================================================
# Minimal production build - excludes desktop-only packages

# Core FastAPI
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Firebase & Cloud
firebase-admin==6.4.0
google-cloud-firestore==2.14.0
google-cloud-logging==3.9.0

# Data processing
pandas==2.2.0
numpy==1.26.3

# NBA API & Data
nba_api==1.4.1
requests==2.31.0

# Database (Cloud SQL PostgreSQL)
asyncpg==0.29.0
sqlalchemy==2.0.25

# Environment
python-dotenv==1.0.0

# Async utilities
asyncio==3.4.3
aiohttp==3.9.1
httpx==0.26.0

# Production WSGI
gunicorn==21.2.0

# Excluded from cloud build (desktop-only):
# - pyserial (desktop hardware)
# - electron-specific packages
# - SQLite drivers (using Cloud SQL instead)
# - sse-starlette (SSE replaced by Firebase)
